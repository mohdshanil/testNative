// import Navigator from "app/src/Navigator";
// import { NavigationActions } from "react-navigation";
 
// const initialState = Navigator.router.getStateForAction(NavigationActions.init({params: 'login'}));

// export default (state = initialState, action) => {
//   // Our Navigator's router is now responsible for
//   // creating our navigation state object
//   return Navigator.router.getStateForAction(action, state) || state;
// };
